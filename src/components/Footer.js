import React from "react";

const Footer = () => {
    return (
        <footer style={{ textAlign: "center", padding: "10px", fontSize: "16px" }}>
            Footer
        </footer>
    );
};

export default Footer;