import React from "react";

const Header = () => {
    return (
        <header style={{ textAlign: "center", padding: "10px", fontSize: "20px" }}>
            Header
        </header>
    );
};

export default Header;